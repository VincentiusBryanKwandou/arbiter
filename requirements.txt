# === Core (Fase 0–5, tanpa uang nyata) ===
pydantic>=2.6
pydantic-settings>=2.2
pyyaml>=6.0
python-dotenv>=1.0
structlog>=24.1
httpx>=0.27

# === Data & numerik ===
numpy>=1.26
pandas>=2.2
polars>=0.20
pyarrow>=15.0          # Parquet
scipy>=1.12

# === Scheduling / async ===
apscheduler>=3.10

# === Polymarket (Fase 1+) ===
# py-clob-client adalah SDK CLOB resmi Polymarket (chain: Polygon).
py-clob-client>=0.17

# === API server (dashboard control) ===
fastapi>=0.111
uvicorn[standard]>=0.30

# === Flash Loan Arb (Fase 6+) — Base mainnet ===
web3>=7.0                  # Ethereum/Base RPC client (async)
eth-abi>=5.0               # ABI encoding for calldata construction
eth-account>=0.12          # Tx signing

# === Dev / test ===
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.4
mypy>=1.9
